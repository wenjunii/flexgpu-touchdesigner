# Runtime-only dependencies for the optional Depth Anything V2 Small sensor
# emulator. PyTorch/torchvision are installed separately from the official
# CUDA wheel index by scripts/Initialize-DepthAnything.ps1.
numpy==2.2.6
opencv-python==4.10.0.84
Pillow==12.3.0
# Transformers 5.5 requires huggingface-hub>=1.5 and has not passed the live
# Depth Anything model audit. Keep this validated 4.x/0.x pair together.
huggingface-hub==0.32.4
transformers==5.5.0
safetensors==0.5.3
